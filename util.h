#include <string>

using namespace std;

void trim(string&);
void toLowerCase(string*);
bool isNumber(string*);
int localExit(string*, int);
string* pointToFilename(char*, bool sansExt = false);
string* removeFromLastOccurrence(char*, char);

